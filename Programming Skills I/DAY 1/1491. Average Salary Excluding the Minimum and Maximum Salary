class Solution:
    def average(self, salary: List[int]) -> float:
        m=max(salary)
        n=min(salary)
        count=0
        for i in salary:
            count+=i
        return (count-m-n)/(len(salary)-2)
